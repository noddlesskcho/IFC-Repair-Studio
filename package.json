{
  "name": "ifc-shape-representation-repair-tool",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static browser tool for repairing missing IFC+SG IfcShapeRepresentation context references.",
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "test": "node --test tests/browser/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
